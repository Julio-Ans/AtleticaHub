# Ambiente
NODE_ENV=development

# Database (PostgreSQL)
DATABASE_URL="postgresql://usuario:senha@localhost:5432/atleticahub"

# MongoDB (se necessário)
MONGODB_URI="mongodb://localhost:27017/atleticahub"

# Firebase Service Account (Produção)
# IMPORTANTE: Baixe o arquivo firebase-service-account.json do Firebase Console
# Em produção, use a variável FIREBASE_SERVICE_ACCOUNT com o JSON completo
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account","project_id":"..."}

# Server
PORT=3000
FRONTEND_URL="http://localhost:3001"
